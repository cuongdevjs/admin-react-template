/*
 * AssetsPage Messages
 *
 * This contains all the text for the AssetsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AssetsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AssetsPage container!',
  },
});
